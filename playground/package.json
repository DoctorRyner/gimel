{
  "name": "examples",
  "license": "ISC",
  "description": "",
  "scripts": {
    "watch": "spago build --watch",
    "start": "parcel static/index.html",
    "release": "rm -r dist; spago build && parcel build static/index.html --no-source-maps"
  },
  "author": "",
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  },
  "dependencies": {}
}
